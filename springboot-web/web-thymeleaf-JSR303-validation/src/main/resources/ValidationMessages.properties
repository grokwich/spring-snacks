# ValidationMessages.properties is the SpringBoot default message bundle for the JSR-303 specification.
# RECOMMENDATION:
# Use the full power of JSR-303 by maintaining all Validation messages here (ValidationMessages.properties). 
# ie. do not maintain Validation messages in messages.properties.
#
# http://beanvalidation.org/1.0/spec/#default-resolution-algorithm

fooCommand.textField.min.message=The Text Field ${validatedValue.length() < 1 ? 'is empty': 'has only ' + validatedValue.length() + 'characters'}, but the length must be at least {min} characters.
fooCommand.textField.max.message=The Text Field value has ${validatedValue.length()}, but the length cannot exceed {max}.

# NOTE: For demo purposes the validation message for textareaField configured in (@see) messages.properties.  

fooCommand.textField.email.message=The Text Field requires an email address.
